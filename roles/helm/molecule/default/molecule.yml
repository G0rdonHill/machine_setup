---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint

platforms:
  - name: ubuntu20
    image: geerlingguy/docker-ubuntu2004-ansible

provisioner:
  name: ansible
  lint:
    name: ansible-lint

verifier:
  name: ansible
