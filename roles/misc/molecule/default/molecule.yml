---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint

platforms:
  - name: ubuntu20
    image: ubuntu:20.04

provisioner:
  name: ansible
  lint:
    name: ansible-lint

verifier:
  name: ansible
